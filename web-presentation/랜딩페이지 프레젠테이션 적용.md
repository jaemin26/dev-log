## 📦 사용된 라이브러리

| 라이브러리 | 버전 | 역할 |
| --- | --- | --- |
| **Three.js** | - | 웹 기반 3D 그래픽 렌더링 엔진 (Core) |
| **@react-three/fiber** | ^9.x | Three.js를 React 컴포넌트처럼 사용할 수 있게 해주는 Reconciler |
| **@react-three/drei** | ^10.x | R3F를 위한 유용한 헬퍼 컴포넌트 모음 (Loader, Environment 등) |
| **framer-motion** | ^11.x | UI 애니메이션 (모달 전환 등) |

### 설치 명령어

```bash
npm install three @react-three/fiber @react-three/drei framer-motion
```

---

## 🗂️ 관련 파일 구조

```
frontend/
├── public/
│   └── ppt/                   # PPT 슬라이드 이미지 (1.png ~ 23.png)
├── src/
│   ├── app/
│   │   └── page.tsx           # 랜딩 페이지 (Canvas, 네비게이션 UI)
│   └── components/
│       └── presentation/
│           └── PresentationScene.tsx  # 3D 씬 (슬라이드 렌더링 로직)
```

---

## 🎬 핵심 코드 설명

### 1. 3D 캔버스 설정 (`page.tsx`)

```tsx
import { Canvas } from "@react-three/fiber";

<Canvas shadows flat camera={{ position: [0, 0, 8], fov: 45 }}>
  <color attach="background" args={["#f5f5f5"]} />
  <Suspense fallback={null}>
    <PresentationScene currentSlide={currentSlide} />
  </Suspense>
</Canvas>;
```

| 속성 | 설명 |
| --- | --- |
| `shadows` | 그림자 렌더링 활성화 |
| `flat` | **ToneMapping OFF** - PPT 이미지 원본 색상 그대로 출력 (중요!) |
| `camera` | 초기 카메라 위치 및 시야각(fov) 설정 |

---

### 2. 슬라이드 “카드 덱” 레이아웃 (`PresentationScene.tsx`)

슬라이드들이 부챗살처럼 펼쳐지는 효과의 핵심 로직입니다.

```tsx
{
  SLIDE_URLS.map((url, i) => {
    const offset = i - currentSlide; // 현재 슬라이드로부터의 거리

    // Card Deck / Cover Flow Layout
    const x = offset * 2.5; // 좌우 간격 (촘촘하게)
    const z = -Math.abs(offset) * 3.5; // 멀어질수록 뒤로 밀려남
    const rY = -offset * 0.25; // 부채꼴 회전

    return (
      <SlideWrapper
        key={url}
        url={url}
        isActive={i === currentSlide}
        targetPosition={[x, 0, z]}
        targetRotation={[0, rY, 0]}
      />
    );
  });
}
```

| 변수 | 효과 |
| --- | --- |
| `x` | 현재 슬라이드 기준 **좌우 배치** |
| `z` | 현재 슬라이드에서 멀수록 **뒤로 밀려남** (깊이감) |
| `rY` | 각 슬라이드가 **비스듬히 회전**하여 부채꼴 형태 연출 |

---

### 3. 부드러운 애니메이션 (`SlideWrapper` 컴포넌트)

슬라이드 전환 시 “슈욱~” 하고 날아오는 효과입니다.

```tsx
useFrame((state, delta) => {
    if (!meshRef.current) return;

    // Position lerp (선형 보간)
    meshRef.current.position.x = THREE.MathUtils.lerp(
        meshRef.current.position.x,
        targetPosition[0],
        delta * 4  // 보간 속도
    );
    meshRef.current.position.y = THREE.MathUtils.lerp(...);
    meshRef.current.position.z = THREE.MathUtils.lerp(...);

    // Rotation lerp
    meshRef.current.rotation.y = THREE.MathUtils.lerp(
        meshRef.current.rotation.y,
        targetRotation[1],
        delta * 4
    );
});
```

| 기술 | 설명 |
| --- | --- |
| `useFrame` | 매 프레임마다 호출되는 애니메이션 루프 훅 (@react-three/fiber) |
| `lerp` | Linear Interpolation - 현재값에서 목표값으로 **부드럽게 전환** |
| `delta * 4` | 속도 조절 (값이 클수록 빨라짐) |

---

### 4. 자동 카메라 거리 조절 (`AutoFitCamera`)

화면 크기에 상관없이 슬라이드가 항상 꽉 차게 보이도록 합니다.

```tsx
const AutoFitCamera = () => {
  useFrame((state, delta) => {
    const aspect = state.viewport.aspect; // 화면 비율
    const fov = state.camera.fov; // 시야각

    // 슬라이드가 화면에 꽉 차도록 필요한 카메라 거리 계산
    const vFovRad = THREE.MathUtils.degToRad(fov);
    const distH = targetH / (2 * Math.tan(vFovRad / 2));
    const distW = targetW / (2 * Math.tan(vFovRad / 2) * aspect);

    const targetZ = Math.max(distH, distW, 8);

    // 부드럽게 카메라 이동
    state.camera.position.z = THREE.MathUtils.lerp(
      state.camera.position.z,
      targetZ,
      delta * 3,
    );
  });
  return null;
};
```

---

### 5. 슬라이드 3D 박스 구조 (`<mesh>`)

```tsx
<mesh>
  <boxGeometry args={[SLIDE_WIDTH, SLIDE_HEIGHT, thickness]} />

  {/* 옆면: 연한 회색 */}
  <meshStandardMaterial attach="material-0" color="#eeeeee" />
  <meshStandardMaterial attach="material-1" color="#eeeeee" />
  <meshStandardMaterial attach="material-2" color="#eeeeee" />
  <meshStandardMaterial attach="material-3" color="#eeeeee" />

  {/* 앞면: PPT 이미지 */}
  <meshBasicMaterial
    attach="material-4"
    map={texture}
    toneMapped={false} // ⭐ 원본 색상 유지
  />

  {/* 뒷면: 흰색 */}
  <meshStandardMaterial attach="material-5" color="#ffffff" />
</mesh>
```

| 속성 | 설명 |
| --- | --- |
| `boxGeometry` | 3D 박스 형태 (가로 x 세로 x 두께) |
| `material-0~5` | 박스의 6면에 개별 재질 적용 |
| `meshBasicMaterial` | 조명 영향 없이 **원본 이미지 그대로** 출력 |
| `toneMapped={false}` | 색감 보정 OFF (중요!) |

---

### 6. 그림자 효과 (`ContactShadows`)

슬라이드가 공중에 떠 있는 느낌을 주는 부드러운 그림자입니다.

```tsx
<ContactShadows
  position={[0, -4, 0]} // 바닥 위치
  opacity={0.65} // 그림자 진하기
  scale={50} // 그림자 범위
  blur={2} // 흐림 정도
  far={10} // 그림자 거리
  color="#000000"
/>
```

---

### 7. 환경 조명 (`Environment`)

```tsx
<Environment preset="city" />
```

`@react-three/drei`에서 제공하는 프리셋으로, 슬라이드 옆면에 은은한 반사 효과를 줍니다.

---

## ⌨️ 키보드 네비게이션 (`page.tsx`)

```tsx
useEffect(() => {
  const handleKeyDown = (e: KeyboardEvent) => {
    if (e.key === "ArrowRight" || e.key === " ") {
      setCurrentSlide((prev) => Math.min(prev + 1, TOTAL_SLIDES - 1));
    } else if (e.key === "ArrowLeft") {
      setCurrentSlide((prev) => Math.max(prev - 1, 0));
    }
  };

  window.addEventListener("keydown", handleKeyDown);
  return () => window.removeEventListener("keydown", handleKeyDown);
}, []);
```

| 키 | 동작 |
| --- | --- |
| `→` (오른쪽 화살표) | 다음 슬라이드 |
| `←` (왼쪽 화살표) | 이전 슬라이드 |
| `Space` (스페이스바) | 다음 슬라이드 |

---

## 💡 커스터마이징 가이드

### 슬라이드 수 변경

```tsx
// PresentationScene.tsx & page.tsx
const TOTAL_SLIDES = 23; // 슬라이드 수에 맞게 수정
```

### 슬라이드 크기 변경

```tsx
// PresentationScene.tsx
const SLIDE_WIDTH = 12.3; // 가로 크기
const SLIDE_HEIGHT = 6.7; // 세로 크기 (16:9 비율 권장)
```

### 애니메이션 속도 변경

```tsx
// SlideWrapper useFrame 내부
delta * 4; // 값을 높이면 빠르게, 낮추면 느리게
```

### 레이아웃 스타일 변경

```tsx
const x = offset * 2.5; // 좌우 간격 증가 → 넓게 펼쳐짐
const z = -Math.abs(offset) * 3.5; // 깊이 증가 → 뒤로 더 밀림
const rY = -offset * 0.25; // 회전 각도 증가 → 더 극적인 부채꼴
```

---

## 📚 참고 자료

- [React Three Fiber 공식 문서](https://docs.pmnd.rs/react-three-fiber)
- [Drei 컴포넌트 목록](https://github.com/pmndrs/drei)
- [Three.js 공식 문서](https://threejs.org/docs/)

---